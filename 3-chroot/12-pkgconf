# Chroot: pkgconf 
# Build under chroot

# Configure source with clang
./configure \
    --prefix=/llvmtools

# Build and install to toolchain
make -j`nproc --all` && make install

# Create a compatibility link
ln -s pkgconf /llvmtools/bin/pkg-config
