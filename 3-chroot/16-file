# Chroot: file
# Build and install under chroot
# Must use same version for llvmtools 
# and final system. 

# astron.com is broken, you may need to find file tarball elsewhere

# Configure source
./configure \
    --prefix=/llvmtools \
    --disable-libseccomp

# Build and install to toolchain
make -j`nproc --all` && make install
