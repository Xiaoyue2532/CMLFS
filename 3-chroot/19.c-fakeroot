# Chroot: fakeroot
# Build under chroot

# Configure source with clang
./configure \
    --prefix=/llvmtools \
    --libdir=/llvmtools/lib/libfakeroot \
    --disable-static

# Build and install to toolchain
make -j`nproc --all` && make install
