# Final System: Libcap
# This section is done in Chroot environment

# Apply patches from Chimera Linux:
patch -Np1 -i ../../CMLFS/patches/libcap-chimera/no-bash.patch
patch -Np1 -i ../../CMLFS/patches/libcap-chimera/remove-so-main.patch

# Prevent static libraries from being installed:
sed -i '/install -m.*STA/d' libcap/Makefile

# Compile with clang
make prefix=/llvmtools lib=lib CC=clang

# Install
make prefix=/llvmtools \
     lib=lib install
