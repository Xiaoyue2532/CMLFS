# Final System: Automake 
# This section is done in Chroot environment

# Configure source with clang
./configure \
    --prefix=/usr \
    --docdir=/usr/share/doc/automake-1.18

# Compile & install
make -j`nproc --all` && make install
